{
  "name": "vaultmind-hnsw-prototype",
  "version": "0.0.1",
  "private": true,
  "description": "Standalone semantic-search prototype for VaultMind. Not part of the published plugin.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "search": "tsx src/build-index.ts",
    "bench": "tsx src/bench.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.0"
  }
}
