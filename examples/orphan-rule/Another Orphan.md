# Another Orphan

A second standalone note. Same situation as `Orphan Note.md` — zero inbound links, so VaultMind reports it.

Use this example to verify orphan detection scales: with two orphans flagged, the Connectivity dimension takes a measurable hit in the health score.
